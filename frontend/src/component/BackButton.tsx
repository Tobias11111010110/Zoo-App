import {Button} from "react-bootstrap";

export default function BackButton() {
    return (
        <div className={"flex justify-center bottom-0 p-10"}>
            <Button href={"/"}>Zurück</Button>
        </div>
    )
}